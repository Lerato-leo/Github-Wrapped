apiVersion: v1
kind: Secret
metadata:
  name: github-wrapped-secrets
  namespace: github-wrapped
  labels:
    app: github-wrapped
type: Opaque
stringData:
  # Add your GitHub token here (optional for public repos)
  github-token: ""
